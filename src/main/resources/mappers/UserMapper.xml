<?xml version="1.0" encoding="UTF-8"?>
        <!DOCTYPE mapper
                PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
                "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.bootproj.pmcweb.Mapper.UserMapper">
    <select id="getUserList" resultType="com.bootproj.pmcweb.Domain.User">
        select * from User
    </select>
    <select id="getUserById" resultType="com.bootproj.pmcweb.Domain.User">
        select * from User
        where id = #{id}
    </select>
</mapper>