<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.bootproj.pmcweb.Mapper.CategoryMapper">
    <select id="selectAllRegions" resultType="com.bootproj.pmcweb.Domain.Region">
        SELECT * FROM region;
    </select>

    <select id="selectAllSubjects" resultType="com.bootproj.pmcweb.Domain.Subject">
        SELECT * FROM subject;
    </select>
</mapper>